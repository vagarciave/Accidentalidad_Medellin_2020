# Source code for model prediction

# Esta funcion recibe la fecha inicio y la fecha fin
# 
check_prediction <- function(fecha_inicio, fecha_fin){
  if(ymd(fecha_))
}
  
# Esta funcion retorna los accidentes observados y/o predichos
control_prediction <- function(fecha_inicio, fecha_fin, lugar)